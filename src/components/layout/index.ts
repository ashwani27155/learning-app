/**
 * Layout Components
 *
 * PublicLayout          — Header + Footer wrapper for public pages
 * AuthLayout            — Centered card layout for login/register pages
 * StudentLayout         — Sidebar + main content for student dashboard
 * AdminLayout           — Admin sidebar + top bar + content area
 * ExamLayout            — Fullscreen, no-distraction layout for test attempt
 *
 * Header                — Public site header
 *   - Logo
 *   - Primary navigation links
 *   - Language toggle (Marathi/English)
 *   - Auth buttons: Login | Register
 *   - Logged-in: avatar dropdown (Dashboard, Profile, Logout)
 *   - Notification bell (for logged-in users)
 *   - Mobile: hamburger menu
 *
 * StudentSidebar        — Student dashboard sidebar
 *   - User avatar + name + exam target
 *   - Navigation items with icons and active indicator
 *   - Subscription badge
 *   - Streak display
 *   - Collapsible on mobile
 *
 * AdminSidebar          — Admin panel sidebar
 *   - Grouped navigation sections:
 *     Overview | Content Management | Question Bank | Users |
 *     Subscriptions & Payments | Analytics | Settings
 *   - Collapsible sub-menus
 *   - Badge for pending approvals count
 *
 * Footer                — Public site footer
 *   - Logo + tagline
 *   - Quick links (About, Contact, Privacy, Terms, Blog)
 *   - Exam categories links
 *   - Social media links
 *   - Newsletter signup
 *   - App store badges (if mobile app exists)
 *   - Copyright
 *
 * PageHeader            — Reusable page title + breadcrumb + CTA area
 * MobileNav             — Bottom tab bar for mobile student app
 */
export {};
