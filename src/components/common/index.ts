/**
 * Common / Shared UI Components
 *
 * Button            — variants: primary, secondary, outline, ghost, danger
 * Input             — with label, error, helper text, icon support
 * Textarea          — with character count
 * Select            — custom styled, searchable
 * Checkbox          — with label
 * Radio             — radio group
 * Toggle            — switch component
 * Badge             — variants: success, warning, error, info, outline
 * Avatar            — with fallback initials, size variants
 * Card              — base card with optional header, body, footer
 * Modal             — with overlay, close on backdrop click, portal
 * Drawer            — side drawer (left/right)
 * Tooltip           — hover tooltip
 * Popover           — click popover
 * Tabs              — horizontal and vertical tabs
 * Accordion         — collapsible sections
 * Alert             — info/success/warning/error alerts
 * Breadcrumb        — navigation breadcrumb
 * Pagination        — with page numbers, prev/next
 * Skeleton          — loading skeleton for cards, text, images
 * Spinner           — loading spinner, sizes: sm/md/lg
 * EmptyState        — empty data state with icon, message, CTA
 * ErrorBoundary     — catch and display UI errors gracefully
 * ProgressBar       — linear progress with label
 * CircularProgress  — circular progress ring
 * Table             — sortable, filterable data table
 * Countdown         — live countdown timer display
 * FileUpload        — drag-and-drop upload with preview
 * RichTextEditor    — rich text editor (with Marathi font support)
 * LanguageToggle    — Marathi ↔ English toggle button
 * SearchInput       — debounced search input with clear button
 * DatePicker        — date and date range picker
 * Rating            — star rating display and input
 * Tag               — dismissible tag/chip component
 * Stepper           — multi-step form wizard indicator
 * ConfirmDialog     — confirmation modal dialog
 * ImageViewer       — full-screen image viewer
 * PDFViewer         — PDF reader component
 * Chart wrappers:
 *   BarChart
 *   LineChart
 *   DonutChart
 *   RadarChart
 *   AreaChart
 */
export { ConfirmDialog } from "./ConfirmDialog";
export { EmptyState } from "./EmptyState";
